namespace SAGE
{
    public class GameExporter
    {
        public void ExportGame(string gameName, string filePath)
        {
            // Export the game to the specified file path
            // Include all necessary game data and metadata
        }

        public void EditMetadata(string filePath, string newGameName, string description, string author)
        {
            // Edit the metadata of the exported game at the specified file path
            // Update the game name, description, and author information
        }
    }
}
